name: Ping
on:
  push:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get scheduled notifications
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: ./ntfy.sh
